"""Mini App API — moved to server.py (Sprint 008).

POST /api/booking is now a Flask route in server.py, served on $PORT.
GCal sync runs via threading.Thread (DECISION-012).
Auth via Telegram WebApp initData HMAC-SHA256 (DECISION-013).
"""
